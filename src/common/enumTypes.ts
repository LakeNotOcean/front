export enum idType {
  customer = "customer",
  order = "order",
  model = "model",
  category = "category",
  none = "none",
}

export enum SendType {
  customer = "customer",
  order = "order",
  model = "model",
  category = "category",
}
